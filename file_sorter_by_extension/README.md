# 🗂️ File Sorter by Extension

Organizes files in a folder by their file type into subfolders (e.g., `.pdf`, `.jpg`).

## 🛠 Built With

- Python
- `os` + `shutil` modules

## ✨ Features

- Moves files to type-based folders
- Creates folder if it doesn't exist
- Skips non-files
